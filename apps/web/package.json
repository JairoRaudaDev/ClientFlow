{
  "name": "@clientflow/web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "node src/dev-server.mjs",
    "build": "tsc --project tsconfig.json --noEmit",
    "lint": "eslint .",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "clean": "node -e \"console.log('@clientflow/web clean skipped; Next.js has not been initialized yet.')\""
  },
  "devDependencies": {
    "@clientflow/config": "workspace:*"
  }
}
