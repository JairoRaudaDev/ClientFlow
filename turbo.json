{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "@clientflow/api#dev": {
      "cache": false,
      "persistent": true,
      "passThroughEnv": [
        "API_HOST",
        "API_PORT",
        "CORS_ORIGIN",
        "DATABASE_URL",
        "JWT_ACCESS_TOKEN_TTL",
        "JWT_AUDIENCE",
        "JWT_ISSUER",
        "JWT_SECRET",
        "NODE_ENV"
      ]
    },
    "@clientflow/web#dev": {
      "cache": false,
      "persistent": true,
      "passThroughEnv": ["HOST", "NODE_ENV", "WEB_PORT"]
    },
    "build": {
      "dependsOn": ["^build"]
    },
    "@clientflow/types#build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "@clientflow/api#build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"]
    },
    "clean": {
      "dependsOn": ["^clean"],
      "cache": false
    }
  }
}
